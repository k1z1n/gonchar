# 🚀 Инструкция по настройке системы характеристик товаров

## ✅ Что было реализовано:

1. **Таблица `product_characteristics`** - для хранения значений характеристик товаров
2. **Ajax endpoint `get_characteristics.php`** - для динамической загрузки характеристик категории
3. **Обработчик `save_product.php`** - для сохранения товара с характеристиками
4. **JavaScript на странице добавления товара** - для динамического показа полей характеристик

---

## 📋 Шаги настройки:

### 1️⃣ Создать таблицу в БД

Выполните SQL запрос из файла `database/create_product_characteristics_table.sql`:

```sql
CREATE TABLE IF NOT EXISTS product_characteristics (
    id INT AUTO_INCREMENT PRIMARY KEY,
    product_id INT NOT NULL,
    characteristic_id INT NOT NULL,
    value VARCHAR(255) NOT NULL,
    FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE,
    FOREIGN KEY (characteristic_id) REFERENCES characteristics(id) ON DELETE CASCADE,
    UNIQUE KEY unique_product_char (product_id, characteristic_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

Можно выполнить через:
- phpMyAdmin
- Консоль MySQL
- Или любой SQL клиент

### 2️⃣ Заполнить шаблоны характеристик

Для каждой категории добавьте шаблоны характеристик в таблицу `characteristics`.

**Как узнать ID категорий:**
```sql
SELECT id, title FROM category;
```

**Пример заполнения для категории с ID=1 (Кружки):**
```sql
INSERT INTO characteristics (name, content, category_id) VALUES
('Объем', 'Укажите объем в мл', 1),
('Материал', 'Материал изготовления', 1),
('Ручка', 'Наличие ручки', 1);
```

**Пример заполнения для категории с ID=2 (Вазы):**
```sql
INSERT INTO characteristics (name, content, category_id) VALUES
('Высота', 'Высота в см', 2),
('Диаметр', 'Диаметр в см', 2),
('Материал', 'Материал изготовления', 2);
```

**Готовые примеры** находятся в файле `database/insert_sample_characteristics.sql`

⚠️ **Важно:** Укажите правильные `category_id` для ваших категорий!

---

## 🎯 Как это работает:

### Процесс добавления товара:

1. **Администратор** открывает `?page=admin_add_product`
2. **Выбирает категорию** из выпадающего списка
3. **JavaScript автоматически** загружает характеристики этой категории
4. **Отображаются поля** для каждой характеристики
5. **Заполняет** название, цену, описание, количество И характеристики
6. **Нажимает "Сохранить"**
7. **Товар сохраняется** в таблицу `products`
8. **Характеристики сохраняются** в таблицу `product_characteristics`

---

## 📊 Структура данных:

```
category (категории)
    ↓
characteristics (ШАБЛОНЫ - какие поля нужны для каждой категории)
    ↓
products (товары) + product_characteristics (ЗНАЧЕНИЯ для каждого товара)
```

**Пример:**

Характеристики категории "Кружки":
- Объем
- Материал  
- Ручка

Для товара "Кружка синяя" сохранится:
- Объем: 250 мл
- Материал: Керамика
- Ручка: Да

---

## 🔍 Проверка работы:

### 1. Проверить в БД:

```sql
-- Посмотреть все характеристики для категории 1
SELECT * FROM characteristics WHERE category_id = 1;

-- Посмотреть характеристики конкретного товара
SELECT 
    c.name,
    pc.value
FROM product_characteristics pc
JOIN characteristics c ON pc.characteristic_id = c.id
WHERE pc.product_id = 1;
```

### 2. Проверить в интерфейсе:

1. Откройте `?page=admin_add_product`
2. Выберите категорию
3. Должны появиться поля для характеристик
4. Заполните и сохраните
5. Проверьте, что данные сохранились в БД

---

## 🛠️ Дальнейшая разработка:

### Вывод характеристик на странице товара:

В файле `pages/product.php` добавьте:

```php
// Получаем характеристики товара
$productId = $_GET['id'] ?? 0;
$sql = "SELECT 
    c.name,
    pc.value
FROM product_characteristics pc
JOIN characteristics c ON pc.characteristic_id = c.id
WHERE pc.product_id = ?";
$stmt = $database->prepare($sql);
$stmt->execute([$productId]);
$productCharacteristics = $stmt->fetchAll();

// Выводим
foreach ($productCharacteristics as $char) {
    echo "<p>{$char['name']}: {$char['value']}</p>";
}
```

### Редактирование товара:

В файле `pages/admin_edit_product.php`:
1. Загрузить существующие характеристики товара
2. При изменении категории - загружать новые шаблоны
3. Обновлять `product_characteristics` вместо создания новых

---

## ❓ Частые вопросы:

**В: Как добавить новую характеристику для категории?**  
О: Вставьте новую запись в таблицу `characteristics` с нужным `category_id`

**В: Как изменить название характеристики?**  
О: UPDATE characteristics SET name='Новое название' WHERE id=X

**В: При удалении категории что будет с характеристиками?**  
О: Нужно вручную удалить записи из `characteristics` с этим `category_id`

**В: Как получить все товары с определенной характеристикой?**  
О: JOIN через `product_characteristics` и `characteristics`

---

## ✅ Готово!

Система готова к использованию. После выполнения шагов 1-2 вы можете добавлять товары с динамическими характеристиками!




